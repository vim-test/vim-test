name:                subpackage3
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

tests:
  single-project-test:
    main:                Spec.hs
    source-dirs:         test
    dependencies:
      - hspec
